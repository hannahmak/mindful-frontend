import styled from 'styled-components';

const Container = styled.div`
`
const Head = styled.p`
`



const HeaderText = ({}) => {
    return <Container>
        
    </Container>
}

export default HeaderText;